{
  "compilerOptions": {
    "lib": [
      "dom",
      "dom.iterable",
      "esnext"
    ],
    "allowJs": true,  // Allows JavaScript files to be compiled.
    "skipLibCheck": true,  // Skips type checking of declaration files to speed up the build.
    "strict": false,  // Disables strict type-checking (you may enable this for more rigorous type checking).
    "noEmit": true,  // Prevents output files (you can change this if you want to emit .js files from .ts).
    "incremental": true,  // Enables incremental compilation to make builds faster.
    "esModuleInterop": true,  // Enables compatibility with CommonJS modules (needed for imports).
    "module": "esnext",  // Specifies that the ESNext module system should be used.
    "moduleResolution": "node",  // Tells TypeScript to use Node.js style module resolution.
    "resolveJsonModule": true,  // Allows importing JSON files as modules.
    "isolatedModules": true,  // Ensures that all files can be transpiled independently.
    "jsx": "react-jsx",  // Use React 17's JSX transform which no longer requires `import React`.
    "plugins": [
      {
        "name": "next"  // Allows Next.js support in TypeScript.
      }
    ]
  },
  "include": [
    "next-env.d.ts",  // Includes the environment types for Next.js.
    ".next/types/**/*.ts",  // Includes types generated by Next.js in the .next directory.
    "**/*.ts",  // Includes all .ts files.
    "**/*.tsx"  // Includes all .tsx files (React component files).
  ],
  "exclude": [
    "node_modules"  // Excludes the node_modules directory from the compilation.
  ]
}
